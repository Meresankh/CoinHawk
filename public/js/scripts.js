function buttonClicked(event) {
    var buttonValue = event;
    alert(buttonValue);
    if (buttonValue == "Bitcoin") {
        window.open("Bitcoin.html");
    }
}
//when button with id Bitcoin is clicked open up Bitcoin.html